import numpy as np
import matplotlib.pyplot as plt



# D = 2
# N = 40
#
# a = np.linspace(-1.,1.,N)
# A1, A2 = np.meshgrid(a, a)
# A = np.stack([A1.reshape(N**2), A2.reshape(N**2)])
# # A = np.random.normal(size=[2, 1000])
# # B = -0.1 * A
# B = 0.1 * np.random.normal(size=[2, N**2])
# plt.quiver(A[0,:], A[1,:], B[0,:], B[1,:])
# plt.plot(A[0,:], A[1,:], '.')
# plt.show()
